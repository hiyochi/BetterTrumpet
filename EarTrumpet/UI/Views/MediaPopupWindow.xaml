<Window x:Class="EarTrumpet.UI.Views.MediaPopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Theme="clr-namespace:EarTrumpet.UI.Themes"
        xmlns:helpers="clr-namespace:EarTrumpet.UI.Helpers"
        Title="Media Controls"
        Width="300"
        Height="185"
        MinHeight="185"
        MaxHeight="445"
        Theme:Options.Scope="Flyout"
        Theme:Options.Source="System"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        WindowStyle="None"
        Opacity="0">

    <Window.Resources>
        <!-- Smooth pop-in animation -->
        <Storyboard x:Key="SlideIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.25">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="MainBorder"
                             Storyboard.TargetProperty="(Border.RenderTransform).(TransformGroup.Children)[0].(TranslateTransform.Y)"
                             From="20" To="0" Duration="0:0:0.35">
                <DoubleAnimation.EasingFunction>
                    <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="MainBorder"
                             Storyboard.TargetProperty="(Border.RenderTransform).(TransformGroup.Children)[1].(ScaleTransform.ScaleX)"
                             From="0.8" To="1" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <BackEase EasingMode="EaseOut" Amplitude="0.4"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="MainBorder"
                             Storyboard.TargetProperty="(Border.RenderTransform).(TransformGroup.Children)[1].(ScaleTransform.ScaleY)"
                             From="0.8" To="1" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <BackEase EasingMode="EaseOut" Amplitude="0.4"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <!-- Smooth fade-out animation -->
        <Storyboard x:Key="SlideOut">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.15">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="MainBorder"
                             Storyboard.TargetProperty="(Border.RenderTransform).(TransformGroup.Children)[0].(TranslateTransform.Y)"
                             From="0" To="8" Duration="0:0:0.15">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="MainBorder"
                             Storyboard.TargetProperty="(Border.RenderTransform).(TransformGroup.Children)[1].(ScaleTransform.ScaleX)"
                             From="1" To="0.95" Duration="0:0:0.15">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="MainBorder"
                             Storyboard.TargetProperty="(Border.RenderTransform).(TransformGroup.Children)[1].(ScaleTransform.ScaleY)"
                             From="1" To="0.95" Duration="0:0:0.15">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <!-- Track change transition -->
        <Storyboard x:Key="TrackChangeOut">
            <DoubleAnimation Storyboard.TargetName="MarqueeText"
                             Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.09">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="MarqueeText"
                             Storyboard.TargetProperty="(TextBlock.RenderTransform).(TranslateTransform.X)"
                             From="0" To="-6" Duration="0:0:0.09">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="ExpandedCover"
                             Storyboard.TargetProperty="Opacity"
                             From="1" To="0.78" Duration="0:0:0.10"/>
        </Storyboard>

        <Storyboard x:Key="TrackChangeIn">
            <DoubleAnimation Storyboard.TargetName="MarqueeText"
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.16">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="MarqueeText"
                             Storyboard.TargetProperty="(TextBlock.RenderTransform).(TranslateTransform.X)"
                             From="6" To="0" Duration="0:0:0.16">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="ExpandedCover"
                             Storyboard.TargetProperty="Opacity"
                             From="0.78" To="1" Duration="0:0:0.18">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <Style x:Key="MediaButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#30FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#50FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SmallMediaButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#80FFFFFF"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#20FFFFFF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#40FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MediaProgressSliderStyle" TargetType="Slider">
            <Setter Property="Height" Value="20"/>
            <Setter Property="Foreground" Value="#FF6B4DE6"/>
            <Setter Property="IsMoveToPointEnabled" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid Background="Transparent">
                            <Border Height="4" CornerRadius="2" Background="#30FFFFFF" VerticalAlignment="Center"/>
                            <Track x:Name="PART_Track" VerticalAlignment="Center">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Background="{TemplateBinding Foreground}">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="2"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Background="Transparent">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="{TemplateBinding Background}"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Width="10" Height="10">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Fill="#FFF7F8FC" Stroke="#40FFFFFF" StrokeThickness="1"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="MainBorder"
            CornerRadius="12"
            Margin="8"
            ClipToBounds="True"
            RenderTransformOrigin="0.5,1"
            Theme:Brush.Background="FlyoutBackground">
        <Border.RenderTransform>
            <TransformGroup>
                <TranslateTransform X="0" Y="0"/>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </TransformGroup>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="3" Opacity="0.5" Color="Black"/>
        </Border.Effect>
        <Grid>
            <!-- Content -->
            <Grid x:Name="ContentGrid" Margin="16,8,16,12" RenderTransformOrigin="0.5,0.5" Panel.ZIndex="100">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Expand Arrow -->
                <Button x:Name="ExpandButton" Grid.Row="0" Width="50" Height="18"
                        HorizontalAlignment="Center" Margin="0,0,0,4"
                        Background="#20FFFFFF" BorderThickness="0" Cursor="Hand"
                        Click="ExpandButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                                CornerRadius="9" Padding="4,2">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#40FFFFFF"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                    <Grid Width="9" Height="5">
                        <Path x:Name="ExpandArrowDown"
                              Data="{x:Static helpers:PhosphorIconData.CaretDown}"
                              Fill="White"
                              Stretch="Uniform">
                            <Path.RenderTransform>
                                <TranslateTransform Y="0"/>
                            </Path.RenderTransform>
                        </Path>
                        <Path x:Name="ExpandArrowUp"
                              Data="{x:Static helpers:PhosphorIconData.CaretDown}"
                              Fill="White"
                              Opacity="0"
                              Stretch="Uniform"
                              RenderTransformOrigin="0.5,0.5">
                            <Path.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform ScaleY="-1"/>
                                    <TranslateTransform Y="0"/>
                                </TransformGroup>
                            </Path.RenderTransform>
                        </Path>
                    </Grid>
                </Button>

                <!-- Expanded Cover Art (hidden by default) -->
                <Grid x:Name="ExpandedCover" Grid.Row="1" Width="202" Height="202"
                      Margin="0,4,0,8" Opacity="0" Visibility="Collapsed"
                      HorizontalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                    <Grid.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform ScaleX="1" ScaleY="1"/>
                            <TranslateTransform Y="0"/>
                        </TransformGroup>
                    </Grid.RenderTransform>
                    <Border Width="200" Height="200" CornerRadius="8"
                            Margin="2,2,0,0" Background="#30000000"/>
                    <Border Width="200" Height="200" CornerRadius="8" ClipToBounds="True">
                        <Grid>
                            <!-- Blurred low-res base layer -->
                            <Image x:Name="ExpandedCoverBlur" Stretch="UniformToFill"
                                   RenderOptions.BitmapScalingMode="HighQuality" Opacity="1">
                                <Image.Effect>
                                    <BlurEffect x:Name="ExpandedCoverBlurEffect" Radius="0"/>
                                </Image.Effect>
                            </Image>
                            <!-- Sharp high-res overlay (used when HD thumbnail available) -->
                            <Image x:Name="ExpandedCoverImage" Stretch="UniformToFill"
                                   RenderOptions.BitmapScalingMode="HighQuality"/>
                            <Border CornerRadius="8">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#20000000" Offset="0"/>
                                        <GradientStop Color="Transparent" Offset="0.3"/>
                                        <GradientStop Color="Transparent" Offset="0.7"/>
                                        <GradientStop Color="#30000000" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>
                        </Grid>
                    </Border>
                </Grid>

                <!-- Marquee Title -->
                <Canvas Grid.Row="2" Height="20" ClipToBounds="True" Margin="0,0,0,2">
                    <TextBlock x:Name="MarqueeText"
                               Text="No media playing"
                               Foreground="White"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Canvas.Left="0"
                               RenderTransformOrigin="0.5,0.5">
                        <TextBlock.RenderTransform>
                            <TranslateTransform X="0"/>
                        </TextBlock.RenderTransform>
                    </TextBlock>
                </Canvas>

                <!-- Progress Bar -->
                <Grid Grid.Row="3" Margin="0,4,0,6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="PositionText" Grid.Column="0" Text="0:00"
                               Foreground="#80FFFFFF" FontSize="10" VerticalAlignment="Center"
                               Margin="0,0,8,0"/>

                    <Slider x:Name="ProgressSlider"
                            Grid.Column="1"
                            Minimum="0"
                            Maximum="1"
                            SmallChange="1"
                            LargeChange="5"
                            Style="{StaticResource MediaProgressSliderStyle}"
                            ValueChanged="ProgressSlider_ValueChanged"
                            PreviewMouseLeftButtonDown="ProgressSlider_PreviewMouseLeftButtonDown"
                            PreviewMouseMove="ProgressSlider_PreviewMouseMove"
                            PreviewMouseLeftButtonUp="ProgressSlider_PreviewMouseLeftButtonUp"
                            LostMouseCapture="ProgressSlider_LostMouseCapture"/>

                    <TextBlock x:Name="DurationText" Grid.Column="2" Text="0:00"
                               Foreground="#80FFFFFF" FontSize="10" VerticalAlignment="Center"
                               Margin="8,0,0,0"/>
                </Grid>

                <!-- Mini Volume Slider -->
                <Grid x:Name="VolumeControl" Grid.Row="4" Margin="0,2,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="VolumeButton" Grid.Column="0" Width="28" Height="28"
                            Style="{StaticResource SmallMediaButtonStyle}"
                            Click="VolumeButton_Click" ToolTip="Mute/Unmute" VerticalAlignment="Center">
                        <Path x:Name="VolumeIcon"
                              Width="12"
                              Height="12"
                              Data="{x:Static helpers:PhosphorIconData.SpeakerHigh}"
                              Fill="#C8FFFFFF"
                              Stretch="Uniform"/>
                    </Button>
                    <!-- Custom volume track (same style as progress bar) -->
                    <Grid Grid.Column="1" VerticalAlignment="Center" Margin="6,0"
                          x:Name="VolumeTrackContainer" Height="20"
                          SizeChanged="VolumeTrackContainer_SizeChanged"
                          MouseLeftButtonDown="VolumeTrack_MouseDown"
                          MouseMove="VolumeTrack_MouseMove"
                          MouseLeftButtonUp="VolumeTrack_MouseUp"
                          Cursor="Hand">
                        <!-- Track background -->
                        <Border Height="4" CornerRadius="2" Background="#30FFFFFF" VerticalAlignment="Center"/>
                        <!-- Track fill -->
                        <Border x:Name="VolumeTrackFill" HorizontalAlignment="Left" Width="0"
                                Height="4" CornerRadius="2" VerticalAlignment="Center">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop x:Name="VolGradient1" Color="#FF6B4DE6" Offset="0"/>
                                    <GradientStop x:Name="VolGradient2" Color="#FF9B7AFF" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        <!-- Thumb -->
                        <Canvas ClipToBounds="False" VerticalAlignment="Center" Height="12">
                            <Ellipse x:Name="VolumeThumb" Width="10" Height="10" Fill="White"
                                     Canvas.Top="1" Canvas.Left="0" IsHitTestVisible="False">
                                <Ellipse.Effect>
                                    <DropShadowEffect BlurRadius="4" ShadowDepth="0" Opacity="0.4" Color="White"/>
                                </Ellipse.Effect>
                            </Ellipse>
                        </Canvas>
                    </Grid>
                    <TextBlock x:Name="VolumeText" Grid.Column="2" Text="100" FontSize="11"
                               Foreground="#80FFFFFF" VerticalAlignment="Center" Width="24"
                               TextAlignment="Right"/>
                </Grid>

                <!-- Shuffle/Repeat and Control Buttons -->
                <Grid Grid.Row="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Shuffle Button -->
                    <Button x:Name="ShuffleButton" Grid.Column="0" Width="32" Height="32"
                            Style="{StaticResource SmallMediaButtonStyle}"
                            HorizontalAlignment="Right" Margin="0,0,8,0"
                            Click="ShuffleButton_Click" ToolTip="Shuffle">
                        <Path x:Name="ShuffleIcon"
                              Width="13"
                              Height="13"
                              Data="{x:Static helpers:PhosphorIconData.Shuffle}"
                              Fill="#90FFFFFF"
                              Stretch="Uniform"/>
                    </Button>

                    <!-- Main Controls -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button x:Name="PrevButton" Width="40" Height="40" Style="{StaticResource MediaButtonStyle}"
                                Click="PrevButton_Click">
                            <Path Width="14" Height="14" Data="{x:Static helpers:PhosphorIconData.SkipBack}"
                                  Fill="White" Stretch="Uniform"/>
                        </Button>

                        <Button x:Name="PlayPauseButton" Width="48" Height="48" Style="{StaticResource MediaButtonStyle}"
                                Margin="8,0" Click="PlayPauseButton_Click">
                            <Path x:Name="PlayPauseIcon"
                                  Width="18"
                                  Height="18"
                                  Data="{x:Static helpers:PhosphorIconData.Play}"
                                  Fill="White"
                                  Stretch="Uniform"/>
                        </Button>

                        <Button x:Name="NextButton" Width="40" Height="40" Style="{StaticResource MediaButtonStyle}"
                                Click="NextButton_Click">
                            <Path Width="14" Height="14" Data="{x:Static helpers:PhosphorIconData.SkipForward}"
                                  Fill="White" Stretch="Uniform"/>
                        </Button>
                    </StackPanel>

                    <!-- Repeat Button -->
                    <Button x:Name="RepeatButton" Grid.Column="2" Width="32" Height="32"
                            Style="{StaticResource SmallMediaButtonStyle}"
                            HorizontalAlignment="Left" Margin="8,0,0,0"
                            Click="RepeatButton_Click" ToolTip="Repeat">
                        <Path x:Name="RepeatIcon"
                              Width="13"
                              Height="13"
                              Data="{x:Static helpers:PhosphorIconData.Repeat}"
                              Fill="#90FFFFFF"
                              Stretch="Uniform"/>
                    </Button>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
