<UserControl x:Class="EarTrumpet.UI.Views.AppItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Theme="clr-namespace:EarTrumpet.UI.Themes"
             xmlns:ctl="clr-namespace:EarTrumpet.UI.Controls"
             xmlns:resx="clr-namespace:EarTrumpet.Properties"
             Height="{DynamicResource Mutable_AppItemCellHeight}"
             ClipToBounds="True"
             IsTabStop="False">
    <Grid Name="GridRoot"
          Background="Transparent"
          RenderTransformOrigin="0.5,0.5"
          PreviewMouseLeftButtonDown="GridRoot_PreviewMouseLeftButtonDown">
        <Grid.RenderTransform>
            <ScaleTransform ScaleX="1" ScaleY="1" />
        </Grid.RenderTransform>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{DynamicResource Mutable_IconCellWidth}" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="{DynamicResource Mutable_VolumeCellWidth}" />
        </Grid.ColumnDefinitions>
        <Grid x:Name="IconCell"
              Grid.Column="0"
              Margin="{StaticResource DeviceOrAppIconMargin}">
            <Border x:Name="AppIconHost"
                    Width="{DynamicResource Mutable_AppIconSize}"
                    Height="{DynamicResource Mutable_AppIconSize}"
                    HorizontalAlignment="Center">
                <Border.Style>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsMuted}" Value="true">
                                <Setter Property="Opacity" Value="{StaticResource MutedOpacity}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Source, ElementName=AppIcon}" Value="{x:Null}">
                                <Setter Property="Theme:Brush.Background" Value="SystemAccent" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <ctl:ImageEx x:Name="AppIcon"
                                 Width="{DynamicResource Mutable_AppIconSize}"
                                 Height="{DynamicResource Mutable_AppIconSize}"
                                 SourceEx="{Binding}"
                                 Stretch="Fill" />
                    <Border>
                        <Border.Style>
                            <Style TargetType="{x:Type Border}">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Source, ElementName=AppIcon}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>

                        <TextBlock Margin="0,-1,0,0"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Theme:Brush.Foreground="Text"
                                   FontSize="{DynamicResource Mutable_AppIconTextFontSize}"
                                   FontWeight="SemiBold"
                                   Text="{Binding IconText}"
                                   TextAlignment="Center" />
                    </Border>
                </Grid>
            </Border>
            <!-- Lock badge: a disabled slider on its own reads as a bug, so the reason
                 for it has to be visible on the row. -->
            <Border Width="10"
                    Height="10"
                    Margin="0,0,-1,-1"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    CornerRadius="5"
                    Theme:Brush.Background="SystemAccent"
                    ToolTip="{x:Static resx:Resources.AppRulesModeLockText}"
                    ToolTipService.InitialShowDelay="0">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsVolumeLocked}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Theme:Brush.Foreground="Text"
                           FontFamily="Segoe MDL2 Assets"
                           FontSize="6"
                           Text="&#xE72E;" />
            </Border>
            <Button Name="MuteButton"
                    Width="{DynamicResource Mutable_AppIconSize}"
                    Height="{DynamicResource Mutable_AppIconSize}"
                    Command="{Binding ToggleMute}"
                    PreviewMouseDown="MuteButton_PreviewMouseDown"
                    ToolTip="{Binding DisplayName}"
                    ToolTipService.Placement="Right"
                    ToolTipService.InitialShowDelay="0"
                    ToolTipOpening="MuteButton_ToolTipOpening"
                    ToolTipClosing="MuteButton_ToolTipClosing"
                    IsTabStop="False">
                <Button.Style>
                    <Style BasedOn="{StaticResource MuteButton}" TargetType="Button">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsMuted}" Value="true">
                                <Setter Property="Opacity" Value="1" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
                <Button.Content>
                    <TextBlock Theme:Brush.Foreground="{Binding (Theme:Brush.Foreground), RelativeSource={RelativeSource AncestorType=Button}}"
                               FontSize="{DynamicResource Mutable_DeviceVolumeTextFontSize}"
                               Opacity="0.8"
                               Style="{StaticResource GlyphTextBlockStyle}"
                               Text="&#xE74F;" />
                </Button.Content>
            </Button>
        </Grid>
        <ctl:VolumeSlider x:Name="VolumeControl"
                          Grid.Column="1"
                          PeakValue1="{Binding PeakValue1}"
                          PeakValue2="{Binding PeakValue2}"
                          Value="{Binding Volume, Mode=TwoWay}">
            <ctl:VolumeSlider.Style>
                <Style BasedOn="{StaticResource {x:Type ctl:VolumeSlider}}" TargetType="{x:Type ctl:VolumeSlider}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsMuted}" Value="True">
                            <Setter Property="Opacity" Value="{StaticResource MutedOpacity}" />
                        </DataTrigger>
                        <!-- A locked volume would snap straight back, so the slider is
                             disabled rather than left to fight the rule under the cursor. -->
                        <DataTrigger Binding="{Binding IsVolumeLocked}" Value="True">
                            <Setter Property="IsEnabled" Value="False" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ctl:VolumeSlider.Style>
        </ctl:VolumeSlider>
        <TextBlock x:Name="VolumeText" Grid.Column="2" Text="{Binding Volume, Mode=OneWay}">
            <TextBlock.Style>
                <Style BasedOn="{StaticResource AppVolumeTextStyle}" TargetType="TextBlock">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsMuted}" Value="True">
                            <Setter Property="Opacity" Value="{StaticResource MutedOpacity}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Grid>
</UserControl>
